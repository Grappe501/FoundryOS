{
  "name": "@foundry/learning-pyramid",
  "version": "0.0.1",
  "private": true,
  "description": "Foundry Learning Pyramid — Definitions → Concepts → Execution → Projects → Mastery",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
