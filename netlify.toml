# FoundryOS — Mission Control (foundryos.com)
# Monorepo: install from root, build platform workspace
[build]
  command = "npm ci --include=dev && npm run build:platform"
  publish = "apps/platform/.next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"
  # Public env values appear in repo/docs by design — not leakable secrets
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_PLATFORM_NAME,NEXT_PUBLIC_APP_URL,NEXT_PUBLIC_SUPABASE_URL"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[context.production]
  environment = { NODE_ENV = "production" }

[context.deploy-preview]
  environment = { NODE_ENV = "staging" }

[context.branch-deploy]
  environment = { NODE_ENV = "staging" }

# Admin + site-engine: separate Netlify sites (future)
# [context.admin]
#   base = "apps/admin"
# [context.site-engine]
#   base = "apps/site-engine"
