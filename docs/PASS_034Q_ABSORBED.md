# PASS-034Q — Absorbed into 040B (DO NOT DEPLOY)

> Local only. Good work — wrong bottleneck if shipped standalone.

## What 034Q Built (local, uncommitted)

- Producer depth overlays (founding, survival, pairings)
- 8 master profiles (`/bourbon/people`)
- Bottle documentary depth fields
- Compare Any Two tool

## Why Not Deploy

Steve/Ernie lock:

```txt
Depth = connections, not content volume
```

034Q adds **longer pages** — valuable as **graph node content**, not as the product itself.

## Merge Path

| 034Q asset | 040B role |
|------------|-----------|
| `bourbon-depth/people.ts` | Person entity nodes |
| `bourbon-depth/producer-depth.ts` | Producer node metadata |
| `bourbon-depth/bottle-depth.ts` | Bottle node metadata |
| Compare Any Two | `competes_with` edge UI |
| People routes | `works_for` / `created_by` edges |

## Ship When

040B Atlas Graph Engine ships with connection panels on every entity type — not before.

See `docs/PASS_040B_ATLAS_GRAPH_ENGINE.md`.
