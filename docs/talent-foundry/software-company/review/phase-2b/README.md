# Phase 2B — Workshop Environment 2.0

Visual-system rebuild of `/workshop`. MAKE interaction from Phase 2A is unchanged. This package is the environment: spatial field, object materials, machine geometry, and the method turn as a change in the room.

Walk the deploy preview, then capture:

```txt
node scripts/capture-workshop-phase-2b.mjs
```

Local:

```txt
WORKSHOP_URL=http://127.0.0.1:3000/workshop node scripts/capture-workshop-phase-2b.mjs
```

| State | File |
|---|---|
| Linger | `desktop-01-linger` / `mobile-01-linger` |
| Bench reachable | `02-bench-reachable` |
| MAKE opening (Pulse) | `03-make-opening` |
| Work notes | `04-work-notes` |
| Active artifact | `05-active-artifact` |
| Completed artifact | `06-completed-artifact` |
| Method turn | `07-method-turn` |
| Equipped | `08-equipped` |
| Draft opening | `09-draft-opening` |
| Motion | `motion/desktop-pulse-reconfiguration.webm` |

Compare to Phase 1.2 and Phase 2 linger stills. Same room. Deeper field. Not a different website.

The bar: if someone can describe it as a really cool website, it is not done.
