/** Canonical MULTIPLY copy. Fictional newer person. Not a cohort. Not a mentor badge. */
export const MULTIPLY_COPY = {
  arrived: 'SOMEONE ELSE IS STUCK WHERE YOU ALREADY WERE.',
  ask: 'What do you give them so they can continue without you?',
} as const;
